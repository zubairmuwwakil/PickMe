# M7 — PickMe AI-Native Scaffolding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Work in the existing checkout on `main`; do not create a branch or worktree.

**Goal:** Make PickMe's canonical ownership of card-decision semantics executable by detecting byte drift across all six source contract copies, while replacing always-loaded prose with compact routers, linked policies, one on-demand authoring skill, a checked-in verification allowlist, and an authoritative repo map.

**Architecture:** `contracts/` remains the canonical home of published contract bytes; the Swift package keeps its existing byte-equality tests and the Kotlin engine gains equivalent JUnit tests against its packaged main/test resources. Root and stack-local `AGENTS.md` files route agents to on-demand detail, `CLAUDE.md` becomes only `@AGENTS.md`, and Swift tests enforce the router budget and link shape. No standalone guard script or new CI job is needed: the checks run inside the existing `swift test` and `./gradlew :core:engine:test` jobs on every push to `main`.

**Tech Stack:** Swift 6 / XCTest / Swift Package Manager, Kotlin / JUnit 5 / Gradle, Markdown agent routers and skills, Claude Code permission settings, GitHub Actions.

**Spec:** `/Users/zub/Documents/Github_Projects/MoneyTalks/docs/superpowers/specs/2026-08-28-ai-native-repos-design.md` (§1–§10 and M7)

## Global Constraints

- **Direct to `main`.** Do not create a branch, worktree, or pull request. Do not run `gh pr create`. Commit small Conventional Commits directly to `main`. Push standalone tasks immediately; Tasks 3–5 form one linked-router group and push together only after every link target exists.
- **No required status checks.** The 2026-08-30 decision withdrew them because required checks block direct pushes. Existing CI remains the signal; do not change branch protection.
- **Preserve concurrent work.** Stage only paths named by the current task. Never use `git add -A`, `git stash`, `git reset`, or checkout-based restoration. If a named path is dirty from another workstream, stop and coordinate before editing it.
- **P1 Compile to delete.** The two Android copy checks retire the shared-contract prose from always-loaded context; the router checks prevent that context from growing back.
- **P2 Compile or demote.** A5 and E1 move to a linked policy. C1 and D3 are restated briefly, never revised.
- **P3 One owner.** PickMe owns all card-decision semantics. `contracts/` owns released bytes; Swift owns semantic behavior; Kotlin and TypeScript are fixture-gated twins.
- **P4 No check without a trigger.** New checks live in test targets already invoked by `.github/workflows/ci.yml`; no workflow edit is required.
- **Every check gets its own test.** Card-catalogue drift, engine-fixture drift, router budget, Claude import shape, policy links, nested-router presence, and the direct-main freedom clause each have a separately named native test.
- **Native idiom only.** Contract drift is XCTest/JUnit, never a new `.mjs` or shell guard.
- **Ignore artifacts.** Never compare `build/`, `.build/`, Gradle caches, derived resources, or other generated build outputs.
- **Router budget:** root `AGENTS.md` has at most 40 non-empty lines; root `AGENTS.md` plus `CLAUDE.md` is at most 2,400 UTF-8 bytes (approximately 600 tokens).
- **Links demote; `@` eagerly imports.** `ECOSYSTEM.md`, `FLEET.md`, `REPO_MAP.md`, and policy files are Markdown links. The only `@` import is the single `@AGENTS.md` line in `CLAUDE.md`.
- **Verification before push:** `(cd Engine && swift test)` and `(cd android && ./gradlew :core:engine:test)` must both pass.
- **Authorship:** never add a `Co-Authored-By` trailer. The user is the sole author.

## File Structure

**Create:**

- `AGENTS.md` — root identity, one command, pointer table, fleet link, and direct-main freedom clause.
- `Engine/AGENTS.md` — Swift semantic authority and contract-change routing.
- `android/AGENTS.md` — Kotlin twin rules and its native check command.
- `catalogue-pipeline/AGENTS.md` — research/provenance and D3 promotion gate.
- `android/core/engine/src/test/kotlin/com/cardcopilot/engine/ContractsSyncTest.kt` — Android packaged-resource byte equality against `contracts/`.
- `Engine/Tests/CardCopilotEngineTests/AgentRouterTests.swift` — root router budget, import, link, nesting, and workflow checks.
- `docs/policies/product-boundaries.md` — demoted A5 and E1 detail.
- `.claude/settings.json` — read-only and verification command allowlist.
- `.claude/skills/card-contract-authoring/SKILL.md` — on-demand contract authoring procedure.
- `REPO_MAP.md` — single-valued artifact placement map for contracts, five source roots, docs, scripts, and release assets.

**Modify:**

- `CLAUDE.md` — replace the current 1,102-character rules file and eager `@ECOSYSTEM.md` import with `@AGENTS.md`.
- `.gitignore` — explicitly ignore `.claude/settings.local.json` while keeping `.claude/settings.json` trackable.

**Do not modify for M7:**

- `FLEET.md` and `.github/dependabot.yml` — already delivered by M6.
- `.github/workflows/ci.yml` — both native test commands are already triggered on pushes to `main`.
- Any contract JSON as part of the M7 semantic change. Contract resource synchronization belongs to the concurrent contract work that selected the canonical new bytes; M7 only adds enforcement.
- Build artifacts under `build/`, `.build/`, `android/**/build/`, `.gradle/`, or `.kotlin/`.

---

### Task 0: Coordination gate and plan commit

The checkout is shared by concurrent agents. At planning time, `contracts/card-catalogue.json` and the Swift copy have SHA-256 `4d0fa2…`, while the Android packaged copy has `4d5781…`; the Android copy is missing the concurrent dual-cap changes. The fixture trio is still identical. There is also an untracked seventh source-looking file at `android/core/engine/src/main/resources/card-catalogue.json`, outside the package resource path. M7 must expose the six-copy drift, but must not decide, relocate, delete, or commit another workstream's catalogue bytes.

**Files:**

- Existing: `docs/superpowers/plans/2026-08-30-m7-pickme.md`
- Inspect only: the six contract source copies

**Interfaces:**

- Produces a clean ownership boundary for later tasks: M7 edits tests/config/docs only; the concurrent contract task owns the current JSON synchronization.

- [ ] **Step 1: Confirm the branch and inspect only M7 collision paths**

Run:

```bash
git branch --show-current
git status --short -- \
  AGENTS.md CLAUDE.md REPO_MAP.md .gitignore .claude/settings.json \
  .claude/skills/card-contract-authoring/SKILL.md \
  Engine/AGENTS.md Engine/Tests/CardCopilotEngineTests/AgentRouterTests.swift \
  android/AGENTS.md android/core/engine/src/test/kotlin/com/cardcopilot/engine/ContractsSyncTest.kt \
  android/core/engine/src/main/resources/card-catalogue.json \
  catalogue-pipeline/AGENTS.md docs/policies/product-boundaries.md
```

Expected: branch is `main`; none of the listed implementation paths is modified by another workstream, and the extra root-level Android resource no longer exists unless its owner has documented why it is a real seventh source. The plan file itself may be untracked.

If any implementation path is dirty, stop. Do not stash, reset, or overwrite it.

- [ ] **Step 2: Re-measure the six source copies without touching them**

Run:

```bash
shasum -a 256 \
  contracts/card-catalogue.json \
  Engine/Sources/CardCopilotEngine/Resources/card-catalogue.json \
  android/core/engine/src/main/resources/com/cardcopilot/engine/card-catalogue.json \
  contracts/engine-fixtures.json \
  Engine/Tests/CardCopilotEngineTests/Fixtures/engine-fixtures.json \
  android/core/engine/src/test/resources/com/cardcopilot/engine/engine-fixtures.json
```

Expected before implementation begins: each three-file group is byte-identical. If the current card-catalogue mismatch remains, stop and ask the owner of the concurrent contract change to finish its Android sync; M7 must not choose which uncommitted byte set is authoritative.

- [ ] **Step 3: Commit and push the reviewed plan only**

Run:

```bash
git add docs/superpowers/plans/2026-08-30-m7-pickme.md
git commit -m "docs(plan): add PickMe M7 implementation plan"
git push origin main
```

Expected: one Conventional Commit on `main`, with no unrelated file staged and no co-author trailer.

---

### Task 1: Compile Android contract-copy drift in the Kotlin engine suite

The Swift side is already correctly compiled in `Engine/Tests/CardCopilotEngineTests/ContractsSyncTests.swift`: it compares canonical `contracts/` bytes to the Swift bundle and fixture copies one file per test. The missing half is Android. Add it in the Kotlin module so `./gradlew :core:engine:test` independently proves that the bytes Android actually packages are the published bytes.

**Files:**

- Create: `android/core/engine/src/test/kotlin/com/cardcopilot/engine/ContractsSyncTest.kt`
- Read only: `Engine/Tests/CardCopilotEngineTests/ContractsSyncTests.swift`
- Read only: `scripts/sync-contracts-into-android.sh`

**Interfaces:**

- Consumes: canonical files `contracts/card-catalogue.json` and `contracts/engine-fixtures.json`.
- Consumes: classpath resources `/com/cardcopilot/engine/card-catalogue.json` and `/com/cardcopilot/engine/engine-fixtures.json`.
- Produces: two separately named JUnit tests inside the already-triggered `:core:engine:test` task.

- [ ] **Step 1: Write the two native drift checks**

Create `android/core/engine/src/test/kotlin/com/cardcopilot/engine/ContractsSyncTest.kt`:

```kotlin
package com.cardcopilot.engine

import java.nio.file.Files
import java.nio.file.Path
import kotlin.io.path.isDirectory
import org.junit.jupiter.api.Assertions.assertArrayEquals
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Test

class ContractsSyncTest {
    private val repoRoot: Path by lazy {
        generateSequence(Path.of("").toAbsolutePath()) { it.parent }
            .firstOrNull { candidate ->
                candidate.resolve("contracts").isDirectory() &&
                    candidate.resolve("android/core/engine").isDirectory()
            }
            ?: error("Could not find the PickMe repository root from ${Path.of("").toAbsolutePath()}")
    }

    private fun assertPackagedResourceMatchesContract(
        contractName: String,
        resourcePath: String,
    ) {
        val expected = Files.readAllBytes(repoRoot.resolve("contracts").resolve(contractName))
        val stream = javaClass.getResourceAsStream(resourcePath)
        assertNotNull(stream, "$resourcePath is missing from the Kotlin engine test classpath")
        val actual = requireNotNull(stream).use { it.readBytes() }

        assertArrayEquals(
            expected,
            actual,
            "$resourcePath has drifted from contracts/$contractName. " +
                "Run scripts/sync-contracts-into-android.sh from the repository root.",
        )
    }

    @Test
    fun `packaged card catalogue matches the published contract byte for byte`() {
        assertPackagedResourceMatchesContract(
            contractName = "card-catalogue.json",
            resourcePath = "/com/cardcopilot/engine/card-catalogue.json",
        )
    }

    @Test
    fun `packaged engine fixtures match the published contract byte for byte`() {
        assertPackagedResourceMatchesContract(
            contractName = "engine-fixtures.json",
            resourcePath = "/com/cardcopilot/engine/engine-fixtures.json",
        )
    }
}
```

The ancestor search makes the test work from Gradle, an IDE, or another checkout path. It intentionally reads only the two source locations named by M7; build copies are never inspected.

- [ ] **Step 2: Run only the new test class**

Run:

```bash
cd android
./gradlew :core:engine:test --tests com.cardcopilot.engine.ContractsSyncTest --rerun-tasks
```

Expected against a synchronized tree: PASS, 2 tests. If the planning-time drift still exists, the card-catalogue test must FAIL with `has drifted from contracts/card-catalogue.json`, while the engine-fixtures test passes. That failure is evidence the check catches the real bug; return to Task 0 rather than copying over another agent's work.

- [ ] **Step 3: Prove both native suites pass**

Run from the repository root:

```bash
(cd Engine && swift test)
(cd android && ./gradlew :core:engine:test)
```

Expected: both commands PASS. Swift's existing `ContractsSyncTests` proves canonical → Swift; the new Kotlin tests prove canonical → Android.

- [ ] **Step 4: Commit and push only the Kotlin guard**

Run:

```bash
git add android/core/engine/src/test/kotlin/com/cardcopilot/engine/ContractsSyncTest.kt
git commit -m "test(contracts): detect Android contract copy drift"
git push origin main
```

Expected: existing `engine-fixtures (swift test)` and `engine-fixtures (gradle test)` CI jobs both run on the push; no workflow or status-check change is needed.

---

### Task 2: Demote A5 and E1 into one linked product-boundaries policy

These are judgment and product-scope rules, not mechanically decidable invariants. Preserve their ratified meaning in on-demand prose, while keeping only the trigger in the root router.

**Files:**

- Create: `docs/policies/product-boundaries.md`

**Interfaces:**

- Produces: one Markdown document linked from `AGENTS.md`, never eagerly imported.
- Preserves: A5 (deep analytics lives on the hub) and E1 (PickMe is one product of four; In Unity is the hub brand).

- [ ] **Step 1: Create the policy**

Create `docs/policies/product-boundaries.md`:

```markdown
# PickMe product boundaries (A5, E1)

**Read when:** changing product scope, adding analytics or dashboards, writing
ecosystem-facing copy, or deciding whether a feature belongs in PickMe.

## A5 — analytics belongs on the hub

PickMe is the card-decision control centre: wallet setup, owner valuations,
checkout recommendations, feedback/reconciliation, one value-recovered figure,
and a small monthly summary. Deep analytics and visualization live on the web hub.
Do not duplicate hub dashboards in Swift or Kotlin.

Existing code is capability, not authorization. In particular,
`Engine/PortfolioAnalyzer` does not authorize a new deep-analytics product surface.

## E1 — PickMe is one product of four

PickMe keeps its own name and identity. It is not the umbrella brand for the
ecosystem. The web unifier is **In Unity**; PickMe, Looply, and MarketLens remain
distinct products.

Write PickMe copy as a standalone card copilot that can connect to the ecosystem,
not as the name of the combined financial product.

## Authority

These are concise restatements of ratified A5 and E1, not revisions. For
cross-repo identity and ownership, read [`ECOSYSTEM.md`](../../ECOSYSTEM.md).
When documents conflict, use the precedence recorded there and stop rather than
averaging incompatible instructions.
```

- [ ] **Step 2: Verify there are no eager imports**

Run:

```bash
rg -n '^@' docs/policies/product-boundaries.md
```

Expected: no output. The policy may contain Markdown links only.

- [ ] **Step 3: Commit and push the demotion target**

Run:

```bash
git add docs/policies/product-boundaries.md
git commit -m "docs(policy): demote PickMe product boundaries"
git push origin main
```

---

### Task 3: Install the canonical router hierarchy and compile its budget

This task replaces the current 1,102-character `CLAUDE.md` plus its eager 3,334-character `ECOSYSTEM.md` import (~1,100 always-loaded tokens) with a compact root router and stack-local detail. The budget, link semantics, nesting, and direct-main workflow become XCTest checks in the already-triggered Engine suite.

**Files:**

- Create: `AGENTS.md`
- Create: `Engine/AGENTS.md`
- Create: `android/AGENTS.md`
- Create: `catalogue-pipeline/AGENTS.md`
- Create: `Engine/Tests/CardCopilotEngineTests/AgentRouterTests.swift`
- Modify: `CLAUDE.md`

**Interfaces:**

- Produces: root router ≤40 non-empty lines and combined root context ≤2,400 UTF-8 bytes.
- Produces: the exact primary check `(cd Engine && swift test) && (cd android && ./gradlew :core:engine:test)`.
- Consumes: `docs/policies/product-boundaries.md`, `REPO_MAP.md`, `ECOSYSTEM.md`, and `FLEET.md` through Markdown links.

- [ ] **Step 1: Write the failing router-contract tests**

Create `Engine/Tests/CardCopilotEngineTests/AgentRouterTests.swift`:

```swift
import Foundation
import XCTest

final class AgentRouterTests: XCTestCase {
    private static let repoRoot = URL(fileURLWithPath: #filePath)
        .deletingLastPathComponent()  // CardCopilotEngineTests/
        .deletingLastPathComponent()  // Tests/
        .deletingLastPathComponent()  // Engine/
        .deletingLastPathComponent()  // repository root

    private func read(_ relativePath: String) throws -> String {
        try String(
            contentsOf: Self.repoRoot.appendingPathComponent(relativePath),
            encoding: .utf8
        )
    }

    func testRootRouterStaysInsideTheAlwaysLoadedBudget() throws {
        let agents = try read("AGENTS.md")
        let claude = try read("CLAUDE.md")
        let nonEmptyLines = agents.split(separator: "\n").filter {
            !String($0).trimmingCharacters(in: .whitespaces).isEmpty
        }

        XCTAssertLessThanOrEqual(nonEmptyLines.count, 40)
        XCTAssertLessThanOrEqual(agents.utf8.count + claude.utf8.count, 2_400)
    }

    func testClaudeOnlyImportsTheCanonicalRouter() throws {
        XCTAssertEqual(try read("CLAUDE.md"), "@AGENTS.md\n")
    }

    func testDemotedContextUsesMarkdownLinksNeverEagerImports() throws {
        let agents = try read("AGENTS.md")
        for target in [
            "REPO_MAP.md",
            "docs/policies/product-boundaries.md",
            "ECOSYSTEM.md",
            "FLEET.md",
        ] {
            XCTAssertTrue(agents.contains("(\(target))"), "Missing Markdown link to \(target)")
        }
        XCTAssertFalse(agents.contains("@ECOSYSTEM.md"))
        XCTAssertFalse(agents.contains("@FLEET.md"))
    }

    func testEveryStackSpecificRouterExists() throws {
        for path in ["Engine/AGENTS.md", "android/AGENTS.md", "catalogue-pipeline/AGENTS.md"] {
            XCTAssertTrue(
                FileManager.default.fileExists(
                    atPath: Self.repoRoot.appendingPathComponent(path).path
                ),
                "Missing nested router \(path)"
            )
        }
    }

    func testFreedomClauseRequiresDirectWorkOnMain() throws {
        let agents = try read("AGENTS.md")
        XCTAssertTrue(agents.contains("Work directly on `main`"))
        XCTAssertTrue(agents.contains("Do not create branches or pull requests"))
    }
}
```

- [ ] **Step 2: Run the new tests to verify the router contract fails**

Run:

```bash
cd Engine
swift test --filter AgentRouterTests
```

Expected: FAIL because root/nested `AGENTS.md` files do not exist and `CLAUDE.md` is not the single import.

- [ ] **Step 3: Create the root router**

Create `AGENTS.md`:

````markdown
# PickMe — agent router

PickMe is the ecosystem's canonical owner of card-decision semantics: checkout
selection, keep/cancel, benefits, and valuation. Released bytes live in
`contracts/`; Swift defines behavior; every twin must pass the shared fixtures.
Uncatalogued cards follow D3's issuer-sourced request/research path, never an open editor.

## One command

```bash
(cd Engine && swift test) && (cd android && ./gradlew :core:engine:test)
```

This is the cross-language card-semantics gate already run by CI on every push.

## Read when you are…

| File | …doing this |
|---|---|
| [`REPO_MAP.md`](REPO_MAP.md) | deciding where a new artifact belongs |
| [`Engine/AGENTS.md`](Engine/AGENTS.md) | changing Swift semantics or contract bytes |
| [`android/AGENTS.md`](android/AGENTS.md) | changing the Kotlin twin or Android resources |
| [`catalogue-pipeline/AGENTS.md`](catalogue-pipeline/AGENTS.md) | researching, importing, or promoting card data |
| [`product-boundaries.md`](docs/policies/product-boundaries.md) | changing scope, analytics, dashboards, or ecosystem copy |
| [`ECOSYSTEM.md`](ECOSYSTEM.md) | doing cross-repo work or checking ownership |

For model and effort routing, read [`FLEET.md`](FLEET.md).

## Freedom

Anything not named here and not caught by the command above is yours to decide.
Prefer acting and letting the check fail over asking. Work directly on `main`.
Do not create branches or pull requests; make a change smaller if it feels too large.
````

- [ ] **Step 4: Create the Swift/contract-authority router**

Create `Engine/AGENTS.md`:

````markdown
# Engine — Swift semantic authority

`contracts/` owns published JSON bytes. This Swift package owns the canonical
behavior that interprets them; C1 permits only fixture-gated twins of the scoring
core. Contract shape or behavior changes land in Swift and shared fixtures before
Kotlin or TypeScript follows.

## Check

```bash
swift test
```

Edit canonical data under `../contracts/`, then use the root sync scripts; do not
hand-edit bundled resource copies. Every published card must retain D3's
issuer-confirmed sourcing bar. Read the
[`card-contract-authoring` skill](../.claude/skills/card-contract-authoring/SKILL.md)
before changing a contract, schema, fixture, or release stamp.

Anything not named here and not caught by `swift test` is yours to decide. Work
directly on `main`; do not create branches or pull requests.
````

- [ ] **Step 5: Create the Kotlin router**

Create `android/AGENTS.md`:

````markdown
# Android — Kotlin twin

The Kotlin engine implements PickMe's Swift-defined card semantics; it does not
invent Android-only rules. Main and test JSON resources are synchronized copies
of `../contracts/`, and the shared fixtures define cross-language conformance.

## Check

```bash
./gradlew :core:engine:test
```

Change canonical data and fixtures under `../contracts/`, implement behavior in
Swift first, then sync with `../scripts/sync-contracts-into-android.sh`. Read the
[`card-contract-authoring` skill](../.claude/skills/card-contract-authoring/SKILL.md)
for the complete order.

Anything not named here and not caught by the command above is yours to decide.
Work directly on `main`; do not create branches or pull requests.
````

- [ ] **Step 6: Create the catalogue-pipeline router**

Create `catalogue-pipeline/AGENTS.md`:

````markdown
# Catalogue pipeline — research and provenance

This Python pipeline may discover and prepare card facts; it never promotes a
card on aggregator evidence alone. D3 requires issuer-confirmed sourcing before
a card becomes `published`, and blocked or uncleared raw material stays out of
the public repository and app bundles.

## Check

```bash
../scripts/check-raw-source-policy.sh
```

Read [`RAW_SOURCE_POLICY.md`](RAW_SOURCE_POLICY.md) before fetching, retaining,
or redistributing source material. Draft classifications are routing evidence,
not verification. Canonical released output belongs in `../contracts/` and then
follows the
[`card-contract-authoring` skill](../.claude/skills/card-contract-authoring/SKILL.md).

Anything not named here and not caught by the command above is yours to decide.
Work directly on `main`; do not create branches or pull requests.
````

- [ ] **Step 7: Replace Claude's rules with the single canonical import**

Replace all of `CLAUDE.md` with exactly:

```markdown
@AGENTS.md
```

Do not keep `@ECOSYSTEM.md`; Markdown links in `AGENTS.md` are the deliberate on-demand replacement.

- [ ] **Step 8: Run the router checks and measure the actual reduction**

Run:

```bash
(cd Engine && swift test --filter AgentRouterTests)
python3 - <<'PY'
from pathlib import Path
after = len(Path("AGENTS.md").read_text()) + len(Path("CLAUDE.md").read_text())
print({"after_chars": after, "after_approx_tokens": round(after / 4)})
PY
```

Expected: 5 tests PASS; `after_chars <= 2400` and approximately ≤600 tokens. The pre-change measurement captured during planning is 4,401 characters / ~1,100 tokens. Record both values for the final report.

- [ ] **Step 9: Confirm all five old CLAUDE rules have a destination**

Map the old content explicitly:

| Old always-loaded content | New outcome |
|---|---|
| Decision-log / precedence pointer | Linked `ECOSYSTEM.md` router trigger |
| `@ECOSYSTEM.md` | Demoted to Markdown link |
| C1 canonical card semantics | Root + `Engine/AGENTS.md`, backed by native sync/fixture tests |
| E1 one product of four | `docs/policies/product-boundaries.md` |
| Shared contract/API-change rule | `Engine/AGENTS.md` + `card-contract-authoring` skill + native tests |
| A5 analytics on hub | `docs/policies/product-boundaries.md` |
| Keep CI green | One root command, already triggered by CI |

Expected: no bullet is deleted without being compiled or demoted.

- [ ] **Step 10: Commit routers, import, and their tests together**

Run:

```bash
git add AGENTS.md CLAUDE.md Engine/AGENTS.md android/AGENTS.md \
  catalogue-pipeline/AGENTS.md \
  Engine/Tests/CardCopilotEngineTests/AgentRouterTests.swift
git commit -m "feat(agents): install PickMe router hierarchy"
```

Expected: the commit is created locally. Do not push yet: the router's skill and
map targets land in Tasks 4–5, and all three commits will be pushed together.

---

### Task 4: Add the verification allowlist and card-contract-authoring skill

The settings file removes prompts for read-only inspection and known-safe verification. The skill carries the detailed contract procedure on demand, including the distinction between canonical bytes and canonical behavior. Neither file grants remote writes or hides release mutations behind an allowlist.

**Files:**

- Create: `.claude/settings.json`
- Create: `.claude/skills/card-contract-authoring/SKILL.md`
- Modify: `.gitignore`

**Interfaces:**

- Produces: valid Claude Code project settings with verification-only `Bash(...)` permissions.
- Produces: a skill whose `description` triggers on card catalogue, schema, fixture, release, and cross-language semantic changes.

- [ ] **Step 1: Explicitly keep local settings personal**

Append this exact rule to `.gitignore` if it is not already present:

```gitignore
.claude/settings.local.json
```

Do not ignore `.claude/` or `.claude/settings.json`.

- [ ] **Step 2: Create the checked-in verification allowlist**

Create `.claude/settings.json`:

```json
{
  "permissions": {
    "allow": [
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(swift test:*)",
      "Bash(./gradlew :core:engine:test:*)",
      "Bash(./gradlew --no-daemon :core:engine:test:*)",
      "Bash(./scripts/release-catalogue.sh --check)",
      "Bash(python3 scripts/validate-catalogue-schema.py)",
      "Bash(scripts/check-id-permanence.sh)",
      "Bash(scripts/check-raw-source-policy.sh)",
      "Bash(node scripts/generate-merchant-pack.mjs --check)"
    ]
  }
}
```

Intentionally absent: sync scripts, release-stamp writes, publishing, commits, pushes, deletion, app deployment, and any command that mutates remote state.

- [ ] **Step 3: Create the on-demand authoring skill**

Create `.claude/skills/card-contract-authoring/SKILL.md`:

```markdown
---
name: card-contract-authoring
description: Use when changing PickMe card catalogue data, contract schemas, engine fixtures, scoring semantics, release stamps, or synchronized Swift/Kotlin contract resources.
---

# Authoring PickMe card contracts

PickMe owns every card-decision fact in the ecosystem. `contracts/` owns the
published bytes; the Swift engine owns their canonical interpretation. Kotlin
and the authorized TypeScript scoring twin must follow the same fixtures.

## Before editing

1. Read `Engine/AGENTS.md` and, for sourced card data, `catalogue-pipeline/AGENTS.md`.
2. Confirm `git branch --show-current` is `main`. Do not create a branch or PR.
3. Inspect `git status --short` for concurrent edits to every file you need. Stop
   rather than overwriting another workstream.
4. For a new or promoted card, enforce D3: issuer-confirmed sources are required;
   third-party data may route research but cannot verify a published card. There
   is no open card editor.

## Change order

1. Edit canonical data/schema under `contracts/`. If behavior changes, implement
   it in `Engine/` and add or update a hand-derived case in
   `contracts/engine-fixtures.json` in the same change.
2. Validate schemas: `python3 scripts/validate-catalogue-schema.py`.
3. Sync checked-in package resources:
   `scripts/sync-contracts-into-engine.sh` and
   `scripts/sync-contracts-into-android.sh`.
4. Run `(cd Engine && swift test)`. The Swift implementation and its contract-copy
   tests must pass before treating a twin as complete.
5. Implement any required Kotlin behavior, then run
   `(cd android && ./gradlew :core:engine:test)` against the same fixture bytes.
6. If any released contract byte changed, bump `catalogueVersion` according to
   compatibility, run `scripts/release-catalogue.sh`, sync Engine resources again,
   and verify with `scripts/release-catalogue.sh --check`.
7. Review the exact six-copy diff. Build outputs under `build/` and `.build/` are
   irrelevant and must never be used as sources.

## Commit and release

Commit the coherent contract, Swift semantics, fixtures, synchronized resources,
and release stamp together using a Conventional Commit with no co-author trailer.
Push `main` only after both native suites pass. Publishing a GitHub contract
release is a separate external mutation: run `scripts/publish-catalogue.sh` only
when the owner explicitly asked to publish, never merely because authoring passed.
```

- [ ] **Step 4: Verify JSON, ignore behavior, and skill frontmatter**

Run:

```bash
python3 -m json.tool .claude/settings.json >/dev/null
git check-ignore -v .claude/settings.local.json
git check-ignore -v .claude/settings.json || true
sed -n '1,8p' .claude/skills/card-contract-authoring/SKILL.md
```

Expected: JSON parses; local settings are ignored; checked-in settings are not ignored; skill output starts with valid `name` and trigger-oriented `description` frontmatter.

- [ ] **Step 5: Commit and push configuration and skill**

Run:

```bash
git add .gitignore .claude/settings.json .claude/skills/card-contract-authoring/SKILL.md
git commit -m "feat(agents): add contract authoring guidance"
```

Expected: the commit is created locally. Do not push yet: `AGENTS.md` already links
`REPO_MAP.md`, which Task 5 must add before this three-commit router group reaches
remote `main`.

---

### Task 5: Write the authoritative repo map without reorganizing working code

PickMe currently has two plan locations and a flat but small `scripts/` directory. M7 needs a single-valued answer for new work, not a cosmetic tree rewrite. Preserve historical `docs/plans/` links and existing script paths; declare `docs/superpowers/` as the home for new specs/plans and map every real source/artifact root.

**Files:**

- Create: `REPO_MAP.md`

**Interfaces:**

- Produces: exact placement rules linked by the root router and checked by `AgentRouterTests`.
- Preserves: all existing file paths; no bulk moves or dead-file deletions.

- [ ] **Step 1: Create the map**

Create `REPO_MAP.md`:

```markdown
# PickMe repo map — where work goes

This file answers one question: **where should a new artifact live?** Follow the
nearest `AGENTS.md` for how to work in that area.

## Canonical data and synchronized copies

| Artifact | Authoritative home | Copies / rule |
|---|---|---|
| Published card contracts and schemas | `contracts/` | Edit here; never author from a bundle/build copy |
| Swift package resources | `Engine/Sources/CardCopilotEngine/Resources/` | Synchronized by `scripts/sync-contracts-into-engine.sh` |
| Shared Swift fixtures | `Engine/Tests/CardCopilotEngineTests/Fixtures/` | Synchronized from `contracts/engine-fixtures.json` |
| Kotlin engine resources | `android/core/engine/src/{main,test}/resources/com/cardcopilot/engine/` | Synchronized by `scripts/sync-contracts-into-android.sh` |
| Build products | `build/`, `.build/`, `android/**/build/` | Generated and ignored; never compare or edit as source |

## Source trees

| Work | Home |
|---|---|
| iOS/watch/widgets/share UI and app integration (Swift/SwiftUI) | `App/` |
| Canonical card-decision behavior (pure Swift) | `Engine/` |
| Local persistence, capture, sync, and composition (Swift/SwiftData) | `Store/` |
| Kotlin twin, Android store, and Android app | `android/` |
| Python catalogue research, provenance, draft promotion, and raw-source controls | `catalogue-pipeline/` |

## Documentation and release artifacts

| New artifact | Home |
|---|---|
| Ratified design/spec | `docs/superpowers/specs/YYYY-MM-DD-<slug>-design.md` |
| Executable implementation plan | `docs/superpowers/plans/YYYY-MM-DD-<slug>.md` |
| Product/architecture policy loaded on demand | `docs/policies/<topic>.md` |
| Issuer/card research | `docs/research/` |
| Privacy, deletion, and App Review material | `docs/compliance/` |
| Social publishing assets | `docs/linkedin/` |
| App Store screenshots and listing assets | `AppStore/` |

`docs/plans/` contains historical pre-Superpowers documents. Keep links working,
but put all new designs and implementation plans under `docs/superpowers/`.

## Automation

Existing automation remains under `scripts/`:

- `sync-contracts-into-{engine,android}.sh` copies canonical contract bytes.
- `release-catalogue.sh` stamps an immutable content-addressed release;
  `publish-catalogue.sh` performs the separate external publication.
- `validate-catalogue-schema.py`, `check-id-permanence.sh`,
  `check-raw-source-policy.sh`, and `generate-merchant-pack.mjs --check` verify
  release/catalogue invariants.
- `generate_*` scripts produce App Store or catalogue artifacts; outputs belong in
  the mapped artifact directory, not beside the script.

Do not add a second home for the same artifact type. If none of these locations
fits, update this map in the same small commit that introduces the new location.
```

- [ ] **Step 2: Verify the router link and all named roots**

Run:

```bash
for path in contracts App Engine Store android catalogue-pipeline docs scripts AppStore; do
  test -e "$path" || { echo "missing $path"; exit 1; }
done
(cd Engine && swift test --filter AgentRouterTests/testDemotedContextUsesMarkdownLinksNeverEagerImports)
```

Expected: every root exists and the router-link test passes.

- [ ] **Step 3: Commit and push the map**

Run:

```bash
git add REPO_MAP.md
git commit -m "docs(repo): map PickMe artifact ownership"
git push origin main
```

Expected: Tasks 3–5 reach remote `main` together, so no pushed commit exposes a
router link to a missing policy, skill, or map.

---

### Task 6: Full verification, audit, and final report

**Files:**

- Inspect: every M7 path
- Do not create or modify additional files

**Interfaces:**

- Produces: evidence that both language gates pass, the contract-copy groups match, the router meets budget, only intended files changed, commits are Conventional, and `main` is pushed.

- [ ] **Step 1: Prove all six source copies are byte-identical**

Run:

```bash
cmp contracts/card-catalogue.json \
  Engine/Sources/CardCopilotEngine/Resources/card-catalogue.json
cmp contracts/card-catalogue.json \
  android/core/engine/src/main/resources/com/cardcopilot/engine/card-catalogue.json
cmp contracts/engine-fixtures.json \
  Engine/Tests/CardCopilotEngineTests/Fixtures/engine-fixtures.json
cmp contracts/engine-fixtures.json \
  android/core/engine/src/test/resources/com/cardcopilot/engine/engine-fixtures.json
```

Expected: no output and exit code 0 for all four comparisons.

- [ ] **Step 2: Run the exact required native suites**

Run:

```bash
(cd Engine && swift test)
(cd android && ./gradlew :core:engine:test)
```

Expected: both PASS. Do not push a fix-up around a failing suite; diagnose and keep the M7 change small.

- [ ] **Step 3: Run supporting validation**

Run:

```bash
python3 -m json.tool .claude/settings.json >/dev/null
scripts/release-catalogue.sh --check
scripts/check-raw-source-policy.sh
node scripts/generate-merchant-pack.mjs --check
git diff --check
```

Expected: every command exits 0. If `release-catalogue.sh --check` fails because the concurrent contract work has not stamped its change, stop; M7 must not smuggle that unrelated release mutation into a docs/test commit.

- [ ] **Step 4: Recalculate the final always-loaded context**

Run:

```bash
python3 - <<'PY'
from pathlib import Path
agents = Path("AGENTS.md").read_text()
claude = Path("CLAUDE.md").read_text()
chars = len(agents) + len(claude)
nonempty = sum(bool(line.strip()) for line in agents.splitlines())
print({"chars": chars, "approx_tokens": round(chars / 4), "agent_nonempty_lines": nonempty})
PY
```

Expected: ≤2,400 characters, ≤~600 tokens, and ≤40 non-empty router lines. Report the exact result against the measured before value: 4,401 characters / ~1,100 tokens.

- [ ] **Step 5: Audit the diff and commit history**

Run:

```bash
git status --short
git log --oneline --decorate -8
git diff origin/main...HEAD -- \
  AGENTS.md CLAUDE.md REPO_MAP.md .gitignore .claude \
  Engine/AGENTS.md Engine/Tests/CardCopilotEngineTests/AgentRouterTests.swift \
  android/AGENTS.md android/core/engine/src/test/kotlin/com/cardcopilot/engine/ContractsSyncTest.kt \
  catalogue-pipeline/AGENTS.md docs/policies/product-boundaries.md \
  docs/superpowers/plans/2026-08-30-m7-pickme.md
```

Expected: no unstaged M7 changes; commits use Conventional Commit prefixes; no commit message contains `Co-Authored-By`; no contract/build artifact was accidentally included by M7.

- [ ] **Step 6: Push `main` and inspect the resulting CI signal**

Run:

```bash
git push origin main
gh run list --workflow CI --limit 3
```

Expected: push succeeds; the CI run triggered for the final M7 commit is visible. Do not create a PR and do not configure required status checks.

- [ ] **Step 7: Report the outcome**

Report all of the following:

1. Exact before/after always-loaded character and approximate token counts.
2. **Compiled:** canonical → Swift copy equality (existing XCTest), canonical → Android card-catalogue equality (new JUnit), canonical → Android engine-fixture equality (new JUnit), plus router budget/import/link/nesting/direct-main contracts (new XCTest).
3. **Demoted:** A5 deep analytics ownership and E1 four-product identity to `docs/policies/product-boundaries.md`; ecosystem and fleet context to Markdown links; contract procedure to the on-demand skill.
4. Native verification results for `swift test` and `./gradlew :core:engine:test`.
5. Commit SHAs pushed to `main` and the CI run status/link if available.
6. The pre-existing live Android catalogue drift discovered during planning and how the owning concurrent workstream resolved it, without attributing those JSON changes to M7.

---

## Self-Review

**Spec coverage.** §1 canonical `AGENTS.md` → Task 3. §2 root router shape and nested routers only for genuine stack differences → Task 3. §3 native invariant ledger and one test per check → Tasks 1 and 3. §4 one command, de-ceremony, register language, freedom clause → Task 3. §5 direct-to-main amendment and no required checks → Global Constraints and every commit step. §6 checked-in settings, local skill, and `REPO_MAP.md` → Tasks 4–5. §7 freshness → already M6; unchanged. §8 `FLEET.md` line → Task 3; existing M6 file unchanged. §9 verification depth / Dependabot → existing CI and M6 Dependabot unchanged; Tasks 1 and 6 use both native suites. §10 cold start → `REPO_MAP.md`, routers, and authoring skill make contract work discoverable without sibling checkout assumptions. M7's explicit contract-copy invariant → Task 1 plus the existing Swift tests. M7's obsolete required-status-check bullet is superseded by the 2026-08-30 ratified decision and is deliberately not implemented.

**Deliverable coverage.** Root and three nested routers → Task 3. `CLAUDE.md` single import → Task 3. A5/E1 policy demotion → Task 2. `.claude/settings.json` → Task 4. `card-contract-authoring` skill → Task 4. `REPO_MAP.md` covering docs, scripts, contracts, AppStore, Swift trees, Kotlin, and Python pipeline → Task 5. Before/after token report → Task 6.

**Trigger audit.** `AgentRouterTests.swift` and the existing Swift contract sync tests run under CI's `swift test`. `ContractsSyncTest.kt` runs under CI's `./gradlew --no-daemon :core:engine:test`. Both workflow jobs trigger on pushes to `main`; no new workflow or script is necessary.

**Placeholder scan.** The plan contains no prohibited placeholder markers, unspecified code step, or deferred implementation. The only conditional steps are explicit safety stops for concurrent edits or stale release bytes.

**Type/path consistency.** Kotlin class name `ContractsSyncTest` matches the Gradle `--tests com.cardcopilot.engine.ContractsSyncTest` filter. Swift class name `AgentRouterTests` matches the `swift test --filter AgentRouterTests` command. Every router link target is created before the full verification task. Canonical paths exactly match the six source copies named in M7; build-artifact paths are explicitly excluded.

**Scope check.** This is one cohesive milestone with three independently reviewable outcomes: executable drift detection, compiled/demoted agent context, and discoverability/configuration. No product feature, contract semantic change, directory reorganization, branch-protection change, or release publication is included.
